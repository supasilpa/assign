<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sup.administrator.homeworkapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StudentregisterActivity" />
        <activity android:name=".studentloginActivity" />
        <activity android:name=".assignmentregActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".loginadmin" />
        <activity android:name=".studhome" />
        <activity android:name=".studsearch" />
        <activity android:name=".profileedit" />
        <activity android:name=".markissue" />
        <activity android:name=".searchmark"></activity>
    </application>

</manifest>